package com.inn.SweetShop.Constants;

/**
 * Application-wide constant messages for the SweetShop system
 */
public class SweetConstants {

    // Generic error message for unexpected exceptions
    public static final String SOMETHING_WENT_WRONG = "Something went wrong";

    // Validation error for invalid or malformed input data
    public static final String INVALID_DATA = "Invalid data";

    // Authentication error for unauthorized access attempts
    public static final String UNAUTHORIZED_ACCESS = "Unauthorized access";

    // Authorization error for admin-only operations
    public static final String ADMIN_ONLY = "This operation requires admin privileges.";
}